# create a separate config WebDriverConfig, which reads this file
webdriver.baseUrl=https://smartdeal.pro

# create a separate config BrowserConfig, which reads this file
browser.type=CHROME
browser.version=112.0
browser.size=1920x1080

# create a separate config RemoteConfig, which reads this file
remote.driver=http://62.113.108.218:4444/wd/hub